export interface ShellyData {
  turn: 'on' | 'off',
  mode: 'color',
  green: number,
  red: number,
  blue: number,
  gain: number,
  brightness: number,
  white: number,
  temp: number,
  effect: number,
  transition: number
}
